import { motion } from "framer-motion";

export default function Contents({ children }) {
  return (
    <div className="mdx-content prose prose-neutral prose-invert w-full max-w-none rounded-xl bg-neutral-900 p-5 lg:max-w-prose">
      {children}
    </div>
  );
}

Hello world
